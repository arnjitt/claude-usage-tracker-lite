{
  "name": "claude-usage-tracker",
  "version": "0.1.3",
  "description": "Portable meters for Claude session / weekly / per-model limits",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon icon-source.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
