{
  "name": "claude-usage-tracker",
  "version": "0.1.0",
  "description": "Portable meters for Claude session / weekly / Fable limits",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon icon-source.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
